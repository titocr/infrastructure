# Earlier documentation

Old page addresses remain available as pointers to current service instructions.
This manual does not mirror application release histories, test reports or retired
implementation plans. Use [application documentation](../sources.md) for those.

The host decisions that still matter are recorded in the current runbooks:
GTD runs in containers with Cloudflare access; its native job remains disabled.
OSCAR uses ordinary private-host networking; the custom relay is retired.

Earlier Infrastructure implementation records remain in Git history. They are not
current recovery instructions. See the [service inventory](../services.md).
