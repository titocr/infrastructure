# Personal OAuth credentials are encrypted with a separately mounted operator key.
services:
  production:
    volumes:
      - ${GTD_MIND_INTEGRATION_SECRET_ROOT:?Set protected integration key directory}:/run/gtd-integrations:ro
